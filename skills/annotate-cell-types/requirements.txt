# annotate-cell-types required dependencies (Python-only default path)
scanpy>=1.9
anndata>=0.10
pandas>=2.0
numpy>=1.24
scipy>=1.11
celltypist>=1.6

# build-h5ad integration
openpyxl>=3.1

# Optional R adapter dependencies (only needed with --mode deep --enable-r-adapters)
# R packages: SingleR, celldex, SingleCellExperiment, zellkonverter, Azimuth, Seurat, scATOMIC
